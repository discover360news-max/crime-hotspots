# Copy this file to .env and fill in values for local development.
# In production, set these in Cloudflare Pages → Settings → Environment Variables.

# Buttondown newsletter API key (server-only secret)
BUTTONDOWN_API_KEY=

# Google Apps Script URL for safety tip submissions (publicly visible)
PUBLIC_SAFETY_TIPS_GAS_URL=
