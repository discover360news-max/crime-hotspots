---
/**
 * Header Component
 * Reusable header with navigation for all pages
 * Trinidad-focused (no dropdowns needed yet)
 */

interface Props {
  isHomepage?: boolean;
}

const { isHomepage = false } = Astro.props;
---

<header class="bg-white shadow-sm sticky top-0 z-40" data-pagefind-ignore>
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="/" class="flex items-center">
        <img src="/assets/images/logo.png" alt="Crime Hotspots logo" class="h-12 md:h-14 w-auto" />
      </a>

      <!-- Mobile subscribe + search + hamburger menu -->
      <div class="flex md:hidden items-center gap-2 ml-auto">
        <button
          id="subscribeToggleMobile"
          class="px-3 py-1.5 rounded-lg border border-rose-600 text-rose-600 hover:bg-rose-50 active:bg-rose-100 transition text-xs font-medium"
          aria-label="Subscribe to our channels"
        >
          Subscribe
        </button>
        <button
          onclick="window.openSearchModal()"
          class="p-2 rounded-lg hover:bg-slate-100 active:bg-slate-200 transition text-slate-700 hover:text-rose-600"
          aria-label="Search"
          title="Search"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </button>
        <button
          id="menuToggle"
          class="inline-flex items-center justify-center p-2 rounded-md text-slate-700 hover:bg-slate-100 active:bg-slate-200 focus:outline-none focus:ring-2 focus:ring-rose-300 transition-all"
          aria-controls="mainNav"
          aria-expanded="false"
        >
          <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>

      <!-- Desktop nav -->
      <nav id="mainNav" class="hidden md:flex items-center gap-4" role="navigation" aria-label="Primary">
        {!isHomepage && (
          <>
            <button onclick="window.openDashboardModal()" class="text-xs text-slate-700 hover:text-rose-600 font-medium cursor-pointer">
              Dashboard
            </button>
            <button onclick="window.openHeadlinesModal()" class="text-xs text-slate-700 hover:text-rose-600 font-medium cursor-pointer">
              Headlines
            </button>
            <button onclick="window.openArchivesModal()" class="text-xs text-slate-700 hover:text-rose-600 font-medium cursor-pointer">
              Archives
            </button>
          </>
        )}

        <button onclick="window.navigateAreas()" class="text-xs text-slate-700 hover:text-rose-600 font-medium cursor-pointer">
          Areas
        </button>

        <a href="/blog/" class="text-xs text-slate-700 hover:text-rose-600 font-medium">
          Blog
        </a>

        <a href="/report/" class="px-4 py-1.5 min-h-[22px] rounded-lg bg-rose-600 text-white hover:bg-rose-700 active:bg-rose-800 transition text-xs font-medium">
          Report a Crime
        </a>

        <a href="/about/" class="text-xs text-slate-700 hover:text-rose-600 font-medium">
          About
        </a>

        <button
          onclick="window.openSearchModal()"
          class="p-2 rounded-lg hover:bg-slate-100 active:bg-slate-200 transition text-slate-700 hover:text-rose-600"
          aria-label="Search"
          title="Search (Ctrl+K)"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </button>

        <button
          id="subscribeToggle"
          class="px-4 py-1.5 rounded-lg border border-rose-600 text-rose-600 hover:bg-rose-50 active:bg-rose-100 transition text-xs font-medium"
          aria-label="Subscribe to our channels"
        >
          Subscribe
        </button>
      </nav>
    </div>

    <!-- Separator (top of pills) - MOBILE ONLY -->
    <div class="h-px bg-gradient-to-r from-transparent via-slate-300 to-transparent md:hidden"></div>

    <!-- Quick Access Pills (horizontal scroll) - MOBILE ONLY -->
    <div class="bg-white md:hidden">
      <div class="relative overflow-hidden">
        <div id="pillsContainer" class="flex items-center gap-2 py-2.5 overflow-x-auto snap-x snap-mandatory hide-scrollbar px-4">
          <a href="/report/"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-rose-600 text-rose-600 hover:bg-rose-700 active:bg-rose-800 transition text-xs font-medium whitespace-nowrap">
            Report Crime
          </a>
          {!isHomepage && (
            <>
              <button onclick="window.openHeadlinesModal()"
                 class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap cursor-pointer">
                Headlines
              </button>
              <button onclick="window.openDashboardModal()"
                 class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap cursor-pointer">
                Dashboard
              </button>
            </>
          )}
          <button onclick="window.navigateAreas()"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap cursor-pointer">
            Areas
          </button>
          <a href="/blog/"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap">
            Blog
          </a>
          <a href="/about/"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap">
            About
          </a>
          <a href="/faq/"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap">
            FAQ
          </a>
          <a href="/methodology/"
             class="flex-shrink-0 snap-start px-4 py-1.5 min-h-[22px] flex items-center justify-center rounded-full border border-slate-300 text-slate-500 hover:border-rose-600 hover:text-rose-600 transition text-xs font-medium whitespace-nowrap">
            Methodology
          </a>
        </div>

        <!-- Fade gradient + pulsing arrow (right side) -->
        <div id="pillsScrollHint" class="absolute right-0 top-0 bottom-0 w-16 bg-gradient-to-l from-white via-white/90 to-transparent pointer-events-none flex items-center justify-end pr-2">
          <svg class="w-4 h-4 text-rose-600 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Separator (bottom of pills) - MOBILE ONLY -->
    <div class="h-px bg-gradient-to-r from-transparent via-slate-300 to-transparent md:hidden"></div>
  </div>
</header>

<!-- Subscribe tray backdrop -->
<div id="subscribeBackdrop" class="fixed inset-0 bg-white/25 backdrop-blur-sm z-40 hidden opacity-0 transition-opacity duration-300" data-pagefind-ignore></div>

<!-- Subscribe tray -->
<div id="subscribeTray" class="fixed top-0 right-0 bottom-0 w-80 bg-white/25 backdrop-blur-lg shadow-2xl rounded-l-2xl z-50 transform translate-x-full invisible transition-transform duration-300 ease-out overflow-y-auto" data-pagefind-ignore>
  <div class="p-6">
    <div class="flex items-center justify-between mb-8">
      <h2 class="text-h3 font-bold text-slate-800">Follow Us</h2>
      <button id="subscribeClose" class="p-2 rounded-full text-slate-600 hover:bg-slate-100 active:bg-slate-200 transition-all" aria-label="Close subscribe menu">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <p class="text-body text-slate-500 mb-6">
      Stay updated with the latest Caribbean crime news and insights.
    </p>

    <div class="space-y-4">
      <a href="https://x.com/crimehotsp0ts" target="_blank" rel="noopener noreferrer"
         class="flex items-center gap-4 p-4 rounded-lg border border-slate-200 hover:border-rose-600 hover:bg-rose-50 active:bg-rose-100 transition-all group">
        <svg class="w-8 h-8 text-slate-700 group-hover:text-rose-600" fill="currentColor" viewBox="0 0 24 24">
          <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
        </svg>
        <div>
          <div class="text-nav font-semibold text-slate-700 group-hover:text-rose-600">X (Twitter)</div>
          <div class="text-small text-slate-400">@crimehotsp0ts</div>
        </div>
      </a>

      <a href="https://facebook.com/caribbeancrimehotspots" target="_blank" rel="noopener noreferrer"
         class="flex items-center gap-4 p-4 rounded-lg border border-slate-200 hover:border-rose-600 hover:bg-rose-50 active:bg-rose-100 transition-all group">
        <svg class="w-8 h-8 text-slate-700 group-hover:text-rose-600" fill="currentColor" viewBox="0 0 24 24">
          <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
        </svg>
        <div>
          <div class="text-nav font-semibold text-slate-700 group-hover:text-rose-600">Facebook</div>
          <div class="text-small text-slate-400">@caribbeancrimehotspots</div>
        </div>
      </a>

      <a href="https://whatsapp.com/channel/0029VbBD2mC3gvWTuWxMO21P" target="_blank" rel="noopener noreferrer"
         class="flex items-center gap-4 p-4 rounded-lg border border-slate-200 hover:border-rose-600 hover:bg-rose-50 active:bg-rose-100 transition-all group">
        <svg class="w-8 h-8 text-slate-700 group-hover:text-rose-600" fill="currentColor" viewBox="0 0 24 24">
          <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
        </svg>
        <div>
          <div class="text-nav font-semibold text-slate-700 group-hover:text-rose-600">WhatsApp Channel</div>
          <div class="text-small text-slate-400">Get updates</div>
        </div>
      </a>

      <a href="https://instagram.com/crimehotspots" target="_blank" rel="noopener noreferrer"
         class="flex items-center gap-4 p-4 rounded-lg border border-slate-200 hover:border-rose-600 hover:bg-rose-50 active:bg-rose-100 transition-all group">
        <svg class="w-8 h-8 text-slate-700 group-hover:text-rose-600" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
        </svg>
        <div>
          <div class="text-nav font-semibold text-slate-700 group-hover:text-rose-600">Instagram</div>
          <div class="text-small text-slate-400">@crimehotspots</div>
        </div>
      </a>
    </div>
  </div>
</div>

<!-- Mobile menu backdrop -->
<div id="mobileMenuBackdrop" class="fixed inset-0 bg-white/25 backdrop-blur-sm z-40 hidden opacity-0 transition-opacity duration-300 md:hidden" data-pagefind-ignore></div>

<!-- Mobile menu overlay -->
<nav id="mobileNav" class="fixed top-0 right-0 bottom-0 w-72 bg-white/25 backdrop-blur-lg shadow-2xl rounded-l-2xl z-50 transform translate-x-full invisible transition-transform duration-300 ease-out md:hidden overflow-y-auto" role="navigation" aria-label="Mobile menu" data-pagefind-ignore>
  <div class="p-6">
    <div class="flex items-center justify-end mb-8">
      <button id="mobileMenuClose" class="p-2 rounded-full text-slate-600 hover:bg-slate-100 active:bg-slate-200 transition-all" aria-label="Close menu">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <div class="space-y-1">
      {!isHomepage && (
        <>
          <button onclick="window.openDashboardModal()" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200 w-full text-left cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
            Dashboard
          </button>

          <button onclick="window.openHeadlinesModal()" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200 w-full text-left cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" />
            </svg>
            Headlines
          </button>

          <button onclick="window.openArchivesModal()" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200 w-full text-left cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4" />
            </svg>
            Archives
          </button>
        </>
      )}

      <button onclick="window.navigateAreas()" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200 w-full text-left cursor-pointer">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
        </svg>
        Areas
      </button>

      <a href="/blog/" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
        </svg>
        Blog
      </a>

      <a href="/report/" class="flex items-center gap-2 text-nav text-rose-400 hover:text-rose-600 font-medium py-3 border-b border-slate-200">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
        Report a Crime
      </a>

      <a href="/about/" class="flex items-center gap-2 text-nav text-slate-700 hover:text-rose-600 font-medium py-3 border-b border-slate-200">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        About
      </a>
    </div>
  </div>
</nav>

<style>
  .hide-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .hide-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
</style>

<script is:inline>
  // Mobile menu
  const menuToggle = document.getElementById('menuToggle');
  const mobileNav = document.getElementById('mobileNav');
  const mobileBackdrop = document.getElementById('mobileMenuBackdrop');
  const mobileMenuClose = document.getElementById('mobileMenuClose');

  function openMobileMenu() {
    if (!mobileBackdrop || !mobileNav || !menuToggle) return;
    mobileBackdrop.classList.remove('hidden');
    mobileNav.classList.remove('translate-x-full', 'invisible');
    mobileNav.classList.add('translate-x-0');
    document.body.style.overflow = 'hidden';

    requestAnimationFrame(() => {
      mobileBackdrop.classList.remove('opacity-0');
      mobileBackdrop.classList.add('opacity-100');
    });

    menuToggle.setAttribute('aria-expanded', 'true');
  }

  function closeMobileMenu() {
    if (!mobileBackdrop || !mobileNav || !menuToggle) return;
    mobileNav.classList.remove('translate-x-0');
    mobileNav.classList.add('translate-x-full');
    mobileBackdrop.classList.remove('opacity-100');
    mobileBackdrop.classList.add('opacity-0');
    document.body.style.overflow = '';

    setTimeout(() => {
      mobileBackdrop.classList.add('hidden');
      mobileNav.classList.add('invisible');
    }, 300);

    menuToggle.setAttribute('aria-expanded', 'false');
  }

  if (menuToggle && mobileNav) {
    menuToggle.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = menuToggle.getAttribute('aria-expanded') === 'true';
      if (isOpen) closeMobileMenu();
      else openMobileMenu();
    });

    if (mobileMenuClose) {
      mobileMenuClose.addEventListener('click', closeMobileMenu);
    }

    if (mobileBackdrop) {
      mobileBackdrop.addEventListener('click', closeMobileMenu);
    }

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && menuToggle.getAttribute('aria-expanded') === 'true') {
        closeMobileMenu();
      }
    });

    mobileNav.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        closeMobileMenu();
      });
    });
  }

  // Subscribe tray
  const subscribeToggle = document.getElementById('subscribeToggle');
  const subscribeToggleMobile = document.getElementById('subscribeToggleMobile');
  const subscribeTray = document.getElementById('subscribeTray');
  const subscribeBackdrop = document.getElementById('subscribeBackdrop');
  const subscribeClose = document.getElementById('subscribeClose');

  function openSubscribeTray() {
    if (!subscribeBackdrop || !subscribeTray) return;
    subscribeBackdrop.classList.remove('hidden');
    subscribeTray.classList.remove('translate-x-full', 'invisible');
    subscribeTray.classList.add('translate-x-0');
    document.body.style.overflow = 'hidden';

    requestAnimationFrame(() => {
      subscribeBackdrop.classList.remove('opacity-0');
      subscribeBackdrop.classList.add('opacity-100');
    });
  }

  function closeSubscribeTray() {
    if (!subscribeBackdrop || !subscribeTray) return;
    subscribeTray.classList.remove('translate-x-0');
    subscribeTray.classList.add('translate-x-full');
    subscribeBackdrop.classList.remove('opacity-100');
    subscribeBackdrop.classList.add('opacity-0');
    document.body.style.overflow = '';

    setTimeout(() => {
      subscribeBackdrop.classList.add('hidden');
      subscribeTray.classList.add('invisible');
    }, 300);
  }

  if (subscribeToggle && subscribeTray) {
    subscribeToggle.addEventListener('click', (e) => {
      e.stopPropagation();
      openSubscribeTray();
    });
  }

  if (subscribeToggleMobile && subscribeTray) {
    subscribeToggleMobile.addEventListener('click', (e) => {
      e.stopPropagation();
      openSubscribeTray();
    });
  }

  if (subscribeClose) {
    subscribeClose.addEventListener('click', closeSubscribeTray);
  }

  if (subscribeBackdrop) {
    subscribeBackdrop.addEventListener('click', closeSubscribeTray);
  }

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && subscribeTray && subscribeTray.classList.contains('translate-x-0')) {
      closeSubscribeTray();
    }
  });

  // Context-aware Areas navigation
  // Known country path prefixes mapped to their areas URLs
  const countryPaths = [
    { prefix: '/trinidad', areasUrl: '/trinidad/areas/' },
    { prefix: '/guyana', areasUrl: '/guyana/areas/' },
    { prefix: '/barbados', areasUrl: '/barbados/areas/' },
    { prefix: '/jamaica', areasUrl: '/jamaica/areas/' },
  ];

  window.navigateAreas = function() {
    const path = window.location.pathname;
    for (const country of countryPaths) {
      if (path.startsWith(country.prefix)) {
        window.location.href = country.areasUrl;
        return;
      }
    }
    // Not on a country page — open island picker modal
    if (typeof window.openAreasModal === 'function') {
      window.openAreasModal();
    }
  };

  // Pills scroll hint
  const pillsContainer = document.getElementById('pillsContainer');
  const pillsScrollHint = document.getElementById('pillsScrollHint');

  if (pillsContainer && pillsScrollHint) {
    function checkPillsScroll() {
      const isScrollable = pillsContainer.scrollWidth > pillsContainer.clientWidth;
      const isAtEnd = pillsContainer.scrollLeft + pillsContainer.clientWidth >= pillsContainer.scrollWidth - 10;

      if (!isScrollable || isAtEnd) {
        pillsScrollHint.style.opacity = '0';
      } else {
        pillsScrollHint.style.opacity = '1';
      }
    }

    pillsContainer.addEventListener('scroll', checkPillsScroll);
    window.addEventListener('resize', checkPillsScroll);

    setTimeout(checkPillsScroll, 100);
  }
</script>
