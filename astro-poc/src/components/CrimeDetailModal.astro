---
/**
 * CrimeDetailModal Component
 * Modal that displays full crime details without navigating away
 * Keeps SEO benefits while improving UX
 */
import ReportIssueModal from './ReportIssueModal.astro';
import NewsletterSignup from './NewsletterSignup.astro';
---

<!-- Modal Backdrop -->
<div id="crimeDetailBackdrop" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden opacity-0 transition-opacity duration-300 pointer-events-none"></div>

<!-- Modal -->
<div id="crimeDetailModal" class="fixed inset-0 z-50 flex items-center justify-center p-4 hidden">
  <div id="crimeDetailContent" class="bg-white/85 dark:bg-[hsl(0_0%_8%)]/85 backdrop-blur-md rounded-2xl shadow-2xl max-w-3xl w-full max-h-[90vh] overflow-y-auto transform scale-95 opacity-0 transition-all duration-300">
    <!-- Modal Header -->
    <div class="sticky top-0 z-10 bg-white/90 dark:bg-[hsl(0_0%_8%)]/90 backdrop-blur-md border-b border-slate-200 dark:border-[hsl(0_0%_18%)] px-6 py-4 flex items-center justify-between rounded-t-2xl">
      <h2 class="text-lg font-bold text-slate-700 dark:text-[hsl(0_0%_92%)]">Crime Details</h2>
      <button id="closeCrimeDetailBtn" class="p-2 rounded-full text-slate-600 dark:text-[hsl(0_0%_55%)] hover:bg-slate-100 dark:hover:bg-[hsl(0_0%_15%)] active:bg-slate-200 dark:active:bg-[hsl(0_0%_20%)] transition-all" aria-label="Close modal">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <!-- Modal Body -->
    <div class="p-6">
      <!-- Headline -->
      <h1 id="modalHeadline" class="text-display font-bold text-slate-700 dark:text-[hsl(0_0%_92%)] mb-4"></h1>

      <!-- Metadata -->
      <div id="modalMetadata" class="flex flex-wrap items-center gap-3 text-xs mb-6"></div>

      <!-- Summary -->
      <div id="modalSummary" class="mb-6"></div>

      <!-- Details Grid -->
      <div id="modalDetails" class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-6 p-4 bg-slate-50 dark:bg-[hsl(0_0%_6%)] rounded-lg"></div>

      <!-- Safety Context -->
      <div id="modalSafetyContext" class="mt-6 mb-6"></div>

      <!-- Related Crimes -->
      <div id="modalRelatedCrimes" class="mb-6"></div>

      <!-- Trending Hotspots -->
      <div id="modalTrendingHotspots" class="mb-6"></div>

      <!-- Action Buttons -->
      <div class="flex flex-col gap-3 pt-4 border-t border-slate-200 dark:border-[hsl(0_0%_18%)]">
        <div class="flex items-center gap-3">
          <a id="modalViewFullBtn" href="#" class="flex-1 px-4 py-2 bg-rose-600 text-white rounded-lg hover:bg-rose-700 active:bg-rose-800 transition text-sm font-medium text-center">
            View Full Page
          </a>
          <a id="modalSourceBtn" href="#" target="_blank" rel="noopener" class="flex-1 px-4 py-2 border border-slate-300 dark:border-[hsl(0_0%_22%)] text-slate-700 dark:text-[hsl(0_0%_85%)] rounded-lg hover:bg-slate-50 dark:hover:bg-[hsl(0_0%_12%)] transition text-sm font-medium flex items-center justify-center gap-2">
            Read Original
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
            </svg>
          </a>
        </div>

        <!-- Report Issue Button -->
        <button id="modalReportIssueBtn" class="w-full px-4 py-1.5 min-h-[22px] flex items-center justify-center gap-2 border border-rose-600 dark:border-rose-500 text-rose-600 dark:text-rose-400 rounded-lg hover:bg-rose-50 dark:hover:bg-rose-950/40 active:bg-rose-100 dark:active:bg-rose-950/60 transition font-medium text-xs">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
          </svg>
          Report Issue with This Crime
        </button>

        <!-- Share Section -->
        <div class="flex items-center gap-2">
          <span class="text-xs text-slate-500 dark:text-[hsl(0_0%_55%)]">Share:</span>
          <button id="shareTwitterBtn" class="p-2 rounded-lg border border-slate-300 dark:border-[hsl(0_0%_22%)] text-slate-600 dark:text-[hsl(0_0%_55%)] hover:bg-slate-50 dark:hover:bg-[hsl(0_0%_12%)] transition" title="Share on X (Twitter)">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
            </svg>
          </button>
          <button id="shareFacebookBtn" class="p-2 rounded-lg border border-slate-300 dark:border-[hsl(0_0%_22%)] text-slate-600 dark:text-[hsl(0_0%_55%)] hover:bg-slate-50 dark:hover:bg-[hsl(0_0%_12%)] transition" title="Share on Facebook">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
              <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
            </svg>
          </button>
          <button id="shareWhatsAppBtn" class="p-2 rounded-lg border border-slate-300 dark:border-[hsl(0_0%_22%)] text-slate-600 dark:text-[hsl(0_0%_55%)] hover:bg-slate-50 dark:hover:bg-[hsl(0_0%_12%)] transition" title="Share on WhatsApp">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
            </svg>
          </button>
          <button id="copyLinkBtn" class="p-2 rounded-lg border border-slate-300 dark:border-[hsl(0_0%_22%)] text-slate-600 dark:text-[hsl(0_0%_55%)] hover:bg-slate-50 dark:hover:bg-[hsl(0_0%_12%)] transition" title="Copy link">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
          </button>
          <span id="copyFeedback" class="text-xs text-emerald-600 opacity-0 transition-opacity">Copied!</span>
        </div>
      </div>

      <!-- Newsletter + Support -->
      <div class="mt-6 pt-6 border-t border-slate-200 dark:border-[hsl(0_0%_18%)] grid sm:grid-cols-2 gap-6">
        <NewsletterSignup variant="footer" source="crime-modal" />

        <div>
          <p class="text-caption font-semibold text-slate-700 dark:text-[hsl(0_0%_92%)] mb-2">Support this project</p>
          <p class="text-caption text-slate-400 dark:text-[hsl(0_0%_55%)] mb-3">
            CrimeHotspots is free, ad-free, and independent. If you find it valuable, you can help keep it that way.
          </p>
          <a
            href="https://ko-fi.com/crimehotspots"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-3 py-1.5 rounded-lg border border-slate-200 dark:border-[hsl(0_0%_20%)] text-slate-600 dark:text-[hsl(0_0%_65%)] text-xs font-medium hover:border-amber-400 hover:text-amber-700 dark:hover:text-amber-400 dark:hover:border-amber-700 transition"
          >
            <svg class="w-4 h-4 text-amber-500" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M23.881 8.948c-.773-4.085-4.859-4.593-4.859-4.593H.723c-.604 0-.679.798-.679.798s-.082 7.324-.022 11.822c.164 2.424 2.586 2.672 2.586 2.672s8.267-.023 11.966-.049c2.438-.426 2.683-2.566 2.658-3.734 4.352.24 7.422-2.831 6.649-6.916zm-11.062 3.511c-1.246 1.453-4.011 3.976-4.011 3.976s-.121.119-.31.023c-.076-.057-.108-.09-.108-.09-.443-.441-3.368-3.049-4.034-3.954-.709-.965-1.041-2.7-.091-3.71.951-1.01 3.005-1.086 4.363.407 0 0 1.565-1.782 3.468-.963 1.904.82 1.832 3.011.723 4.311zm6.173.478c-.928.116-1.682.028-1.682.028V7.284h1.77s1.971.551 1.971 2.638c0 1.913-.985 2.667-2.059 3.015z"/>
            </svg>
            Support the Project
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Hidden ReportIssueModal - Values will be updated dynamically -->
<ReportIssueModal
  idPrefix="modal"
  crimeSlug=""
  crimeHeadline=""
  crimeDate=""
  crimeType=""
  crimeArea=""
  crimeRegion=""
  crimeStreet=""
  crimeSummary=""
  crimeSource=""
  crimeUrl=""
/>

<script>
  // === Imports from existing shared modules ===
  import { escapeHtml } from '../lib/escapeHtml';
  import { validateUrl } from '../lib/escapeHtml';
  import { getSafetyContext } from '../lib/safetyHelpers';
  import { buildRoute } from '../config/routes';

  // === Imports from extracted modules ===
  import { generateSafetyContextHTML, generateRelatedCrimesHTML, generateFeedbackToggleHTML, generateTrendingHotspotsHTML } from '../scripts/modalHtmlGenerators';
  import { initModalFeedbackToggle } from '../scripts/modalFeedbackHandler';
  import { initShareHandlers } from '../scripts/modalShareHandlers';
  import { initReportIssueHandler } from '../scripts/modalReportHandler';
  import { createModalLifecycle } from '../scripts/modalLifecycle';

  // === DOM References ===
  const backdrop = document.getElementById('crimeDetailBackdrop')!;
  const modal = document.getElementById('crimeDetailModal')!;
  const contentEl = document.getElementById('crimeDetailContent')!;
  const closeBtn = document.getElementById('closeCrimeDetailBtn');

  // === State ===
  let currentCrimeUrl = '';
  let currentCrimeHeadline = '';
  let currentCrimeData: any = null;

  // === Initialize modules ===
  const lifecycle = createModalLifecycle({ backdrop, modal, content: contentEl });
  initShareHandlers(() => currentCrimeUrl, () => currentCrimeHeadline);
  initReportIssueHandler(() => currentCrimeData);

  // === Close handlers (backdrop + close button) ===
  closeBtn?.addEventListener('click', () => lifecycle.close());
  backdrop?.addEventListener('click', () => lifecycle.close());

  // === Bind related crime links for in-place navigation ===
  function bindRelatedCrimeLinks() {
    document.querySelectorAll('#modalRelatedCrimes .related-crime-link').forEach(link => {
      link.addEventListener('click', (e) => {
        const slug = (link as HTMLElement).getAttribute('data-related-crime-slug');
        const crimes = (window as any).__crimesData || [];
        const crime = crimes.find((c: any) => c.slug === slug);
        if (crime) {
          e.preventDefault();
          openCrimeModal(crime);
        }
      });
    });
  }

  // === Main open function ===
  function openCrimeModal(crime: any) {
    const isAlreadyOpen = lifecycle.isOpen();

    // Format date
    const formattedDate = new Date(crime.date).toLocaleDateString('en-US', {
      year: 'numeric', month: 'long', day: 'numeric'
    });

    // Populate headline
    document.getElementById('modalHeadline')!.textContent = crime.headline;

    // Populate metadata
    let relatedCrimesPills = '';
    if (crime.relatedCrimeTypes?.trim()) {
      relatedCrimesPills = crime.relatedCrimeTypes.split(',')
        .map((c: string) => c.trim())
        .map((rc: string) => `<span class="px-3 py-1 rounded-full bg-slate-200 dark:bg-[hsl(0_0%_20%)] text-slate-600 dark:text-[hsl(0_0%_55%)] font-medium">${escapeHtml(rc)}</span>`)
        .join('');
    }
    document.getElementById('modalMetadata')!.innerHTML = `
      <time class="text-slate-500 dark:text-[hsl(0_0%_55%)]">${escapeHtml(formattedDate)}</time>
      <span class="px-3 py-1 rounded-full bg-slate-200 dark:bg-[hsl(0_0%_20%)] text-slate-600 dark:text-[hsl(0_0%_55%)] font-medium">${escapeHtml(crime.crimeType)}</span>
      ${relatedCrimesPills}
      <span class="text-slate-500 dark:text-[hsl(0_0%_55%)]">${crime.street ? `${escapeHtml(crime.street)}, ` : ''}${escapeHtml(crime.area)}</span>
    `;

    // Populate summary
    const summaryEl = document.getElementById('modalSummary')!;
    summaryEl.innerHTML = crime.summary
      ? `<p class="text-base text-slate-700 dark:text-[hsl(0_0%_82%)] leading-relaxed whitespace-pre-line">${escapeHtml(crime.summary).replace(/\|\|/g, '\n\n')}</p>`
      : '';

    // Populate details grid
    document.getElementById('modalDetails')!.innerHTML = `
      <dt class="text-xs font-semibold text-slate-500 dark:text-[hsl(0_0%_55%)]">Region:</dt>
      <dd class="text-xs text-slate-700 dark:text-[hsl(0_0%_82%)]">${escapeHtml(crime.region)}</dd>
      ${crime.source ? `
        <dt class="text-xs font-semibold text-slate-500 dark:text-[hsl(0_0%_55%)]">Source:</dt>
        <dd class="text-xs text-slate-700 dark:text-[hsl(0_0%_82%)]">${escapeHtml(crime.source)}</dd>
      ` : ''}
    `;

    // Safety context
    const allCrimes = (window as any).__crimesData || [];
    const safetyContext = getSafetyContext(crime.area, allCrimes, crime.primaryCrimeType || crime.crimeType);
    const safetyContextEl = document.getElementById('modalSafetyContext');
    if (safetyContextEl) {
      const feedbackUrl = buildRoute.crimeAbsolute(crime.slug);
      safetyContextEl.innerHTML = generateSafetyContextHTML(crime.area, safetyContext)
        + generateFeedbackToggleHTML(crime.area, feedbackUrl, crime.headline);
      initModalFeedbackToggle(safetyContextEl.querySelector('.feedback-toggle') as HTMLElement);
    }

    // Related crimes
    const relatedCrimesEl = document.getElementById('modalRelatedCrimes');
    if (relatedCrimesEl) {
      relatedCrimesEl.innerHTML = generateRelatedCrimesHTML(crime, allCrimes, 4);
      bindRelatedCrimeLinks();
    }

    // Trending hotspots (hot areas + recent views)
    const trendingEl = document.getElementById('modalTrendingHotspots');
    if (trendingEl) {
      trendingEl.innerHTML = generateTrendingHotspotsHTML(crime, allCrimes);
    }

    // Action buttons
    (document.getElementById('modalViewFullBtn') as HTMLAnchorElement).href = buildRoute.crime(encodeURIComponent(crime.slug));
    const sourceBtn = document.getElementById('modalSourceBtn') as HTMLAnchorElement;
    const safeUrl = validateUrl(crime.url);
    if (safeUrl) {
      sourceBtn.href = safeUrl;
      sourceBtn.style.display = 'flex';
    } else {
      sourceBtn.style.display = 'none';
    }

    // Update state
    currentCrimeUrl = buildRoute.crimeAbsolute(crime.slug);
    currentCrimeHeadline = crime.headline;
    currentCrimeData = crime;

    // History + animation
    if (isAlreadyOpen) {
      lifecycle.replaceUrl(crime.slug);
      contentEl.scrollTop = 0;
    } else {
      lifecycle.setOriginalUrl(window.location.pathname + window.location.search);
      lifecycle.open();
      lifecycle.pushUrl(crime.slug);
    }
  }

  // === Open by slug (for map markers and external calls) ===
  function openCrimeDetailModal(slug: string) {
    const crimes = (window as any).__crimesData || [];
    const crime = crimes.find((c: any) => c.slug === slug);
    if (crime) openCrimeModal(crime);
    else console.error('Crime not found:', slug);
  }

  // === Global exports ===
  (window as any).openCrimeModal = openCrimeModal;
  (window as any).openCrimeDetailModal = openCrimeDetailModal;
</script>
