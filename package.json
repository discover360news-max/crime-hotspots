{
  "name": "crime-hotspots-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generateBlogManifest.js && node scripts/generateRSS.js && node scripts/generateSitemap.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "dompurify": "^3.3.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
